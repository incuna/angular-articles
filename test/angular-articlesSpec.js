/* global beforeEach, describe, expect, module, it */
(function (angular) {

    'use strict';

    beforeEach(function () {
        module('articles');
    });

    describe('Unit: articles', function () {

        describe('routes', function () {

            it('should succeed the tests to begin with', function () {
                expect(true);
            });

        });

    });

}(window.angular));
