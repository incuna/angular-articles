/**
* angular-articles Module
*
* Articles of content for AngularJS.
*/
(function (angular) {
    'use strict';

    var module = angular.module('articles', [
        'ngRoute'
    ]);

}(window.angular));
